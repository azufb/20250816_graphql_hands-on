// MutationのResolverを実装する
import { Member, MemberInput } from "../types/member.types";
import * as MemberService from "../service/member.service";

/**
 * addMember
 * メンバー追加処理
 *
 * // TODO：service呼び出し実装後、返り値の型修正する
 */
export async function addMember(input: MemberInput): Promise<any> {
  // TODO：service呼び出し

  return;
}
